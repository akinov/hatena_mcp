{
  "name": "hatena-bookmark-mcp",
  "version": "1.0.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "axios": "^1.9.0",
    "oauth-1.0a": "^2.2.6",
    "zod": "^3.25.46"
  },
  "scripts": {
    "build": "npx tsc",
    "client": "npm run build && node dist/client.js",
    "test": "npm run build && npx jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.0.0"
  }
}
